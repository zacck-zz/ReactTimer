var React = require('react');

var Timer = React.createClass({
  render: function() {
    return(
      <div>
        <p>Timer</p>
      </div>
    );
  }
});

//export modules
module.exports = Timer;
